# Vision replay data
Place non-versioned logs or videos in `normal_light`, `low_light`, `rotated`, `partial_occlusion`, `motion_blur`, `short_loss`, and `long_loss`. Large recordings are intentionally excluded from Git. Protocol logs may be replayed with `scripts/benchmark/replay_target_protocol.py`.
