# PX4 v1.16 SITL测试报告

日期：2026-07-29。

## 版本门控

搜索到 `/home/xixi/PX4-Autopilot`，实际HEAD为
`d6f12ad1c4f70ad3230afd7d86e971421e02fef4`，不等于批准的
`6ea3539157ca358c70a515878b77077af7d4611d`。该仓库还包含用户修改和未跟踪文件。
因此没有切换、清理、修改或下载PX4；真实SITL测试按规则停止。

## 结果

| 测试 | 结果 | 说明 |
|---|---|---|
| 只读通信 | 未执行 | 缺少精确PX4 v1.16.0源码 |
| hover_test闭环 | 未执行 | 不用错误版本替代 |
| drop闭环 | 未执行 | ROS场景和状态机代码已构建/单测 |
| dynamic_land闭环 | 未执行 | ROS场景和状态机代码已构建/单测 |
| PX4故障注入 | 未执行 | 纯逻辑安全故障已单测 |

普通x500 Gazebo地面不是移动降落平台。本场景未来只能验证状态机、视觉闭环方向、
任务时序、安全保护和二次起飞逻辑，不能证明移动平台接触、摩擦或起飞稳定性。

## 已完成的非PX4验证

- 隔离构建：`uav_vision`、`uav_control` 两包成功。
- 全量测试：149项，0 error，0 failure，1项既有skip。
- 第二阶段专用测试：48项全部通过。
- 无PX4烟雾：状态 `WAIT_PX4`、事件 `RESET`，任务画布约10Hz。
- `enable_control=false` 时三个PX4输入topic各监听均未收到消息。
- 纯逻辑故障注入覆盖：failsafe、异常倾角、90秒超时、D点越过、进度倒退、
  CommandAck拒绝/超时、payload ack超时、视觉失效清零、单帧触地拒绝和人工退出
  Offboard不重新抢权。
